# 项目开发规则（Codex Agent 适用）

本文件是本仓库内所有 Codex Agent、子代理和人工开发者的共同开发约束。进入任何子目录前，Agent 必须先遵守本文件；如后续子目录新增 `AGENTS.md`，其更具体的规则优先。

## 1. 项目目标与边界

本项目建设一个**前后端分离的 A 股模拟量化交易系统**，用于数据研究、策略回测、模拟撮合、风险控制和交易复盘。

- 默认目标是模拟交易与研究，不接入真实资金或自动实盘下单。
- 免费数据是默认数据模式；所有数据源必须可替换，不能将业务逻辑绑定到单一免费接口。
- 不承诺免费数据具备稳定、低延迟或 Level-2 实时能力；界面和接口必须能暴露数据来源、更新时间、延迟和质量状态。
- A 股关键规则（交易日、T+1、涨跌停、停复牌、交易单位、费用税费等）属于核心领域逻辑，必须集中建模并可测试，不得散落在界面或策略代码中。

## 2. 开发原则

1. **先规格，后实现。** 修改前先明确目标、输入输出、验收标准和影响范围；重大设计先更新需求文档或 ADR。
2. **正确性优先于功能数量。** 回测、撮合、账本和风险控制若无法保证可复现和可解释，不得以“先跑起来”为理由跳过规则。
3. **领域逻辑与基础设施解耦。** 策略、撮合、账本、风控不直接依赖数据库、HTTP 框架或第三方数据 SDK。
4. **可复现优先。** 任一次回测必须可定位到策略版本、参数、数据快照、交易规则、费用模型、滑点模型和运行时间。
5. **小步提交，持续验证。** 每个 Agent 只改动完成当前任务所需的最小范围，并在交付前运行相关检查。
6. **安全默认值。** 未明确授权时不得接入真实交易、保存敏感凭证、调用外部下单接口，或执行破坏性数据操作。

## 3. 推荐代码边界

在项目初始化后，优先采用以下逻辑分层；命名可调整，但职责不可混淆：

```text
frontend/              # 独立前端 SPA：展示与交互，不承载交易规则
backend/
  app/api/             # REST / WebSocket 控制器、请求校验、鉴权
  app/application/     # 用例编排：回测任务、下单流程、结算流程
  app/domain/          # 纯领域模型：订单、成交、持仓、费用、风控、撮合
  app/infrastructure/  # 数据库、缓存、消息队列、文件存储、第三方 SDK
  app/providers/       # 行情/基础数据适配器及规范化
  tests/               # 单元、集成、端到端测试
docs/                  # 需求、ADR、接口约定、运行手册
```

- 前端只能通过公开 API 读取和写入数据，不能直接抓取数据源。
- API 层只做鉴权、请求校验与响应转换，不能包含撮合、费用或风控判断。
- 策略只产生“目标仓位”或“订单意图”；是否下单由风控和订单服务决定。
- 数据提供方适配器返回统一模型；上游字段、代码格式、复权和时间语义必须在适配层处理。
- 长时间运行的采集、回测和日终结算必须异步化，不得阻塞 HTTP 请求线程。

## 4. A 股与数据规则

### 4.1 时间与数据

- 所有时间使用带时区的时间戳；市场日历按 `Asia/Shanghai` 处理。
- 行情必须携带 `source`、`observed_at`、`published_at`（可得时）、`granularity`、`quality_status` 等元数据。
- 回测不得读取回测时点之后才发布的数据；财报、公告、指数成分股和 ST 状态必须按“当时可得”日期生效。
- 原始数据只追加、不原地覆盖；清洗数据与原始数据分层保存。
- 证券主数据必须记录上市、退市、停复牌、板块和证券状态，避免幸存者偏差。

### 4.2 撮合与账本

- 订单、成交、持仓、资金流水必须是独立实体，有不可变流水与可审计关联 ID。
- 所有金额、价格、数量使用十进制精度类型；禁止使用二进制浮点数保存或累计货币金额。
- 撮合模型必须明确数据粒度和假设，例如：日线次日开盘成交、分钟 bar 内成交或价格优先近似；报告中展示该假设。
- T+1、最小交易单位、涨跌停、停牌、费用和税费必须配置化，并通过单元测试覆盖。
- 默认拒绝无法确定合法成交的订单；不可静默“补成交”或穿透涨跌停价格。

### 4.3 数据许可与隐私

- 每新增数据源，须在 `docs/data-sources.md` 记录来源、字段范围、授权/条款、更新频率、延迟和降级方案。
- 不将 API Key、Cookie、账号、证券账户信息或个人数据提交到仓库、日志、测试快照或前端代码。
- 使用 `.env.example` 说明必需配置；真实 `.env` 必须纳入 `.gitignore`。

## 5. API 与前端规则

- 使用版本化 API，例如 `/api/v1/...`；公开接口需维护 OpenAPI 文档。
- 写接口使用请求 ID 或幂等键；下单、撤单、结算等命令必须可安全重试。
- API 错误采用统一结构，包含稳定的机器可读错误码、用户可读消息和追踪 ID。
- WebSocket 只推送状态变化和行情增量；客户端重连后必须可通过 REST 恢复完整状态。
- 前端不得根据展示逻辑重新计算关键交易规则；所有关键金额和可交易数量以服务端返回为准。
- 图表、指标和表格必须标示数据周期、来源与最后更新时间；模拟结果必须标示“模拟，不代表实盘成交”。

## 6. 测试与验收规则

### 6.1 最低测试要求

- 新增或修改领域逻辑时，必须新增或更新单元测试。
- 每项 A 股规则至少有一个正常案例和一个边界/拒绝案例。
- 修改 API 时，必须验证请求校验、授权、成功响应和至少一个失败响应。
- 修改数据适配器时，必须用固定 fixture 测试字段映射、时区、复权口径和异常降级。
- 修改前端交互时，至少验证加载、空状态、错误状态和核心成功路径。

### 6.2 强制回归案例

以下案例是撮合/账本相关改动的最小回归集：

1. 当日买入后尝试卖出，应被 T+1 规则拒绝。
2. 涨停价格买入、跌停价格卖出，应按模型规则拒绝或排队，不能默认成交。
3. 停牌证券下单，应拒绝并保持资金/持仓不变。
4. 佣金最低收费、印花税和过户费应精确入账。
5. 除权除息、分红或送配后，持仓数量、成本和资金流水可核对。
6. 同一请求重复提交，不得产生重复订单或重复扣款。

## 7. Git 与文件规则

- 开始编码前检查工作区状态；保留用户已有的未提交修改，不覆盖、不回滚无关文件。
- 每次改动应聚焦单一目的；避免无关格式化、大规模重命名或依赖升级。
- 不执行 `git reset --hard`、强制覆盖、批量删除或其他不可恢复操作，除非用户明确要求。
- 新增依赖必须说明用途、许可证、版本约束和替代方案；优先使用成熟、维护活跃且许可兼容的依赖。
- 生成物、缓存、下载行情、数据库文件、密钥和本地运行日志不得提交；统一维护 `.gitignore`。
- 文档、代码注释、提交信息优先使用简体中文；标识符、API 字段和代码命名使用英文。

## 8. Codex Agent 协作规则

### 8.1 接手任务时

1. 阅读本文件及当前目录下更具体的 `AGENTS.md`。
2. 检查与任务有关的需求、API、测试和现有改动。
3. 在动手前用一句话说明范围、假设和验证方式；不明确但可安全推进的事项采用保守默认值并注明。
4. 若需求涉及真实交易、付费数据采购、外部账户、删除数据或明显扩大范围，停止并请求用户授权。

### 8.2 实现过程中

- 只处理被分配的子任务；发现相邻问题时记录，不擅自扩大改造范围。
- 多 Agent 并行时，不同时编辑同一文件；需要共享接口时，先约定契约再分别实现。
- 不把“无测试的代码”交给其他 Agent 作为完成成果。
- 不将第三方数据接口的临时抓取逻辑直接耦合到领域层；必须放在 Provider Adapter 中。

### 8.3 交付时

交付说明必须包含：

- 已修改的文件与实现结果；
- 运行过的验证命令及结果；
- 仍存在的限制、假设或未覆盖风险；
- 下一步需要用户决策的事项（如有）。

## 9. 文档与决策记录

- 需求变更更新对应 Markdown 文档；重大架构选择在 `docs/adr/` 新增 ADR。
- ADR 至少包含：背景、决策、备选方案、取舍、影响与回滚/迁移方式。
- 对数据、撮合和风控模型的任何假设必须在代码和用户可见报告中可追溯。
- 示例策略仅用于演示与测试；不得表达收益保证或投资建议。

## 10. Definition of Done

一个开发任务只有同时满足以下条件才可标记完成：

- 需求范围已实现，且没有越权扩大范围；
- 代码遵循上述层次和数据/交易规则；
- 相关测试、静态检查或构建已运行并通过；
- 关键用户路径与异常路径已验证；
- 文档、配置样例和数据源登记已在需要时同步更新；
- 交付说明已如实列出验证结果及剩余限制。
